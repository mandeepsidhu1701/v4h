const styles = (theme) => ({
  metawheelText: {
    marginTop: '1rem',
    marginBottom: '6rem',
    textAlign: 'center',
    fontSize: '1rem',
    fontWeight: 500,
    letterSpacing: '4px'
  }
});

export default styles;
