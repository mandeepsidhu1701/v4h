const styles = (theme) => ({
  carousel: {
    position: 'relative',
    width: '100%',
    height: 'auto',
    overflow: 'hidden',
    boxShadow: '-1px 2px 10px 2px rgba(0, 0, 0, 0.5)',
  },
  metawheelText: {
    marginTop: '1rem',
    marginBottom: '6rem',
    textAlign: 'center',
    fontSize: '1rem',
    fontWeight: 500,
    letterSpacing: '4px'
  }
});

export default styles;
